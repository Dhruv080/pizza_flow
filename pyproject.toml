[project]
name = "pizzaflow"
version = "0.1.0"
description = "PizzaFlow - SliceMatic ordering system (FDE Batch C2 applied project)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
